[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx prisma generate && npx prisma migrate deploy && npm run start:prod"

[variables]
NODE_ENV = "production"
