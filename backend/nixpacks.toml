[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "chmod +x start.sh && ./start.sh"

[variables]
NODE_ENV = "production"
